A lightweight command-line expense tracker in Python.
Add, list, delete, and summarize expenses — safely stored in expenses.json. It is inspired from the Expense Tracker project featured in the Backend Roadmap from roadmap.sh. https://roadmap.sh/projects/expense-tracker

## Features

-Add, list, delete, and summarize expenses  
-Atomic JSON saving (prevents file corruption)  
-Month-based summaries  
-Formatted table output with tabulate  
